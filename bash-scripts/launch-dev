docker kill Website-Dev-HTTPS
docker rm Website-Dev-HTTPS

docker build https://github.com/ModernMAK/Personal-Website.git#development -f dockerfiles/https.dockerfile -t personal-website-dev-https

docker run -d --rm --name Website-Dev-HTTPS -v "/sockets:/sockets" personal-website-dev-https
