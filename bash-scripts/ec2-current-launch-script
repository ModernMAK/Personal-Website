sudo usermod -a -G docker ec2-user
sudo service docker start

docker kill Website
docker rm Website
docker rmi $(docker images -q)

docker build https://github.com/ModernMAK/Personal-Website.git#main -t personal-website

docker run -d --name Website -p 80:8080 personal-website